# Route all @GemSphere-AI packages to the GitHub Packages registry
@GemSphere-AI:registry=https://npm.pkg.github.com/

# Authenticate with the registry using a read-only GitHub token
//npm.pkg.github.com/:_authToken=${GITHUB_READ_PACKAGES_TOKEN}
